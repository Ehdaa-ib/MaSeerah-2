/// One row in a matching challenge (left ↔ right).
class ChallengeMatchingPair {
  const ChallengeMatchingPair({
    required this.left,
    required this.right,
  });

  final String left;
  final String right;
}
